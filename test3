$webform_path = \Drupal::service('extension.path.resolver')
  ->getPath('webform');

$styles = [];

foreach (glob($webform_path . '/css/*.css') as $file) {
  $styles[] = base_path() . $file;
}

return $styles;
